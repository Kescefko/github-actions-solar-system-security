name: Testing Self-Hosted Runner

on: 
  workflow_dispatch:

jobs:
    testing:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Content
              run: |
                echo ok
                sleep 15s